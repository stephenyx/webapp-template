{
  "name": "@repo/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx seed/index.ts",
    "db:seed:reset": "tsx seed/reset.ts && tsx seed/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.0",
    "postgres": "^3.4.3",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.3.0",
    "drizzle-kit": "^0.20.0",
    "tsx": "^4.7.0"
  }
}
